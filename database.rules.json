{
  "rules": {
    "locations": {
      ".read": true,
      ".write": true,
      "$location": {
        ".validate": "newData.hasChildren(['latitude', 'longitude', 'timestamp', 'userAgent', 'ip'])"
      }
    }
  }
}
