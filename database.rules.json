{
  "rules": {
    ".read": true,
    ".write": true,
    "locations": {
      "$location": {
        ".validate": "newData.hasChildren(['timestamp', 'userAgent', 'ip'])"
      }
    },
    "test-write": {
      ".validate": "newData.hasChild('timestamp')"
    }
  }
}
